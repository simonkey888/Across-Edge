# ORDER-001 hard safety settings. Never change to true under this order.
SEND_RELAYS=false
SEND_TRANSACTIONS=false
SEND_SLOW_RELAYS=false
RELAYER_USE_INVENTORY_MANAGER=false
# Upstream CLI MUST additionally use: --wallet void
# Public/free RPC URLs may be supplied locally only after their zero-charge status is verified.
# Do not commit URLs containing API keys.
